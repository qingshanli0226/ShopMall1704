<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.shaomall">
    <!-- 联网的权限: -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 写SD卡权限: -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- 读SD卡权限: -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- 读取联系人的权限： -->
    <uses-permission android:name="android.permission.READ_CONTACTS" /> <!-- 读取短信的权限： -->
    <uses-permission android:name="android.permission.READ_SMS" />

    <application
        android:name=".app.ShaoHuaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!--        商品详情-->
        <activity
            android:name=".goodsinfo.GoodsInfoActivity"
            android:launchMode="singleTask"
            android:supportsPictureInPicture="true" />


        <!-- 欢迎界面 -->
        <activity
            android:name=".activity.WelcomeActivity"
            android:supportsPictureInPicture="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 主界面 -->
        <activity
            android:name=".activity.MainActivity"
            android:launchMode="singleTask"
            android:supportsPictureInPicture="true" />

        <!-- 登录 -->
        <activity
            android:name=".login.LoginActivity"
            android:launchMode="singleTask"
            android:supportsPictureInPicture="true" />

        <!-- 注册 -->
        <activity
            android:name=".login.SigninActivity"
            android:launchMode="singleTask"
            android:supportsPictureInPicture="true" />

        <!-- 服务 -->
        <service android:name=".cache.CacheService" />


        <!-- 适配华为（huawei）刘海屏 -->
        <meta-data
            android:name="android.notch_support"
            android:value="true" /> <!-- 适配小米（xiaomi）刘海屏 -->
        <meta-data
            android:name="notch.config"
            android:value="portrait|landscape" />
        <meta-data
            android:name="android.max_aspect"
            android:value="2.4" />
    </application>
</manifest>